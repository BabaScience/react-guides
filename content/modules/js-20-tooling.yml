# js-20-tooling
#
# Display text (module name, step titles, exercise names/descriptions/hints)
# lives in platform/src/i18n/locales/*.json, keyed by the ids below.
#
# Every `## ` heading in the guide must appear exactly once under `steps`
# or `skipSections`. `npm run validate` fails otherwise — that is what makes
# an unreachable section impossible rather than merely unlikely.
id: js-20-tooling
track: javascript
number: 20
status: available
difficulty: intermediate
prerequisites:
  - js-12-modules
guide: arguments/chapters/js-20-tooling.md
exerciseDir: ""
skipSections:
  - Table of Contents
steps:
  - lesson: package-managers
    section: 1. Package Managers
  - lesson: bundlers
    section: 2. Bundlers
  - lesson: linters-and-formatters
    section: 3. Linters and Formatters
  - lesson: monorepo-tools
    section: 4. Monorepo Tools
