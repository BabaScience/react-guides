# rn-05-styling-layout
#
# Display text (module name, step titles, exercise names/descriptions/hints)
# lives in platform/src/i18n/locales/*.json, keyed by the ids below.
#
# Every `## ` heading in the guide must appear exactly once under `steps`
# or `skipSections`. `npm run validate` fails otherwise — that is what makes
# an unreachable section impossible rather than merely unlikely.
id: rn-05-styling-layout
track: react-native
number: 5
status: available
guide: arguments/chapters/rn-05-styling-layout.md
exerciseDir: ""
skipSections:
  - Table of Contents
steps:
  - lesson: core-mechanics
    section: 1. Core Mechanics
  - lesson: responsive-design
    section: 2. Responsive Design
  - lesson: styling-libraries
    section: 3. Styling Libraries
  - lesson: theming
    section: 4. Theming
  - lesson: icons-and-assets
    section: 5. Icons and Assets
